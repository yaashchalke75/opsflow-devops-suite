# When VITE_USE_MOCK=true, the app uses the in-memory mock API (no backend needed).
# Flip to false once the backend is running — the frontend API client will call VITE_API_URL.
VITE_USE_MOCK=true
VITE_API_URL=http://localhost:4000/api
